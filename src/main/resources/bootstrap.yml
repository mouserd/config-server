spring:
  application:
    name: configserver
  cloud:
    config:
      discovery:
        enabled: true
      server:
        bootstrap: true
        git:
          uri: https://github.com/mouserd/config-repo